import type { PluginDefinition } from '../types.js';

export const inventoryPlugin: PluginDefinition = {
  id: 'inventory',
  name: 'Inventory',
  version: '0.1.0',
  description: 'Tracks stock allocation and availability for sellable items.',
};
